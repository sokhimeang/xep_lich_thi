<%= image_tag @user.image.thumb.url %>
<br>
<% @subjects.each do |subject| %>
  <div>
    <%= subject.id %>
    | <%= subject.name %>
    | <%= link_to "Delete", user_subject_url, method: :delete,
      data: { confirm: t("sure") } %>
  </div>
<% end %>
