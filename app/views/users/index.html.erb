<% @users.each do |user| %>
  <div class="row"><%= "#{user.code} : #{user.name}" %>
    | <%= link_to t(".edit"), "/users/#{user.id}/edit" %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: { confirm: t(".sure") } %>
  </div>
<% end %>

<%= will_paginate %>
